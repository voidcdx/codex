import React from 'react';
import ReactDOM from 'react-dom/client';
import AlchemyPage from './AlchemyPage';
import './index.css';

ReactDOM.createRoot(document.getElementById('alchemy-root')!).render(
  <React.StrictMode>
    <AlchemyPage />
  </React.StrictMode>
);
